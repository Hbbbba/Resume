\documentclass[$fontsize$,letterpaper]{article}

% --- Compact page (no extra packages) ---
\setlength{\oddsidemargin}{-0.10in}
\setlength{\evensidemargin}{-0.10in}
\setlength{\textwidth}{6.7in}
\setlength{\topmargin}{-0.55in}
\setlength{\textheight}{9.6in}
\setlength{\parindent}{0pt}
\setlength{\parskip}{2.5pt}

% --- Section style: ALL CAPS like the sample ---
\makeatletter
\renewcommand\thesection{}
\renewcommand\thesubsection{}
\renewcommand\section{\@startsection{section}{1}{0pt}%
  {-0.9ex plus -0.2ex minus -0.2ex}%
  {0.35ex plus 0.1ex}%
  {\normalfont\bfseries\MakeUppercase}}
\makeatother

% --- Compact lists (no enumitem) ---
\makeatletter
\let\olditemize\itemize
\let\endolditemize\enditemize
\renewenvironment{itemize}{
  \olditemize
  \setlength{\itemsep}{1pt}
  \setlength{\parsep}{0pt}
  \setlength{\parskip}{0pt}
  \setlength{\topsep}{2pt}
}{
  \endolditemize
}
\makeatother

% --- Hyperlinks if present (won't error if absent) ---
\AtBeginDocument{%
  \providecommand{\hypersetup}[1]{}%
  \hypersetup{hidelinks}%
}

\begin{document}

% ===== Header (centered) =====
\begin{center}
{\LARGE\bfseries $title$}\par
\vspace{4pt}
$for(header-includes)$
$header-includes$
$endfor$
\end{center}

% Left-aligned separator (like your PDF)
\vspace{2pt}
\hrule
\vspace{10pt}

$body$

\end{document}
